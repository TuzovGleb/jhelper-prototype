# Прототип нового лендинга jhelper.ru

Статический HTML/CSS-прототип пересборки сайта по `План_пересборки_сайта_jhelper.md`.
Дизайн-система снята один-в-один с боевого сайта — новых цветов, шрифтов и радиусов не вводилось.

**Назначение:** согласовать структуру и тексты визуально, затем отдать разработчику как эталон вёрстки для переноса в Next.js.

---

## Как открыть

**Онлайн:** https://tuzovgleb.github.io/jhelper-prototype/

**Локально** — из корня `Code/jhelper`:

```bash
python -m http.server 8010 --bind 127.0.0.1 --directory prototype
```

Дальше — `http://localhost:8010`. Конфигурация уже добавлена в `.claude/launch.json` под именем `jhelper-prototype`.

### Про ссылки

Ссылки внутри сайта — относительные (`tarify/`, `../oferta/`, `../assets/…`). Так страницы работают и на GitHub Pages под подпутём `/jhelper-prototype/`, и на домене-корне.

**При переносе на jhelper.ru URL получаются ровно те, что нужны:** `/tarify`, `/oferta`, `/privacy`, `/rekvizity` — те самые адреса, которые проверяет мониторинг платёжного сервиса. Относительные ссылки на домене-корне резолвятся в них один в один, менять ничего не надо.

---

## Что внутри

```
index.html            лендинг
tarify/index.html     → /tarify
oferta/index.html     → /oferta
privacy/index.html    → /privacy
rekvizity/index.html  → /rekvizity
assets/
  tokens.css   :root, reset, типографика, .container-x .card-x .app-header .logo .btn .sec-eyebrow
  landing.css  блоки лендинга
  legal.css    типографика юр-страниц
  app.js       .scrolled на шапке, мобильное меню, якоря с учётом залипающей шапки
```

### Порядок блоков лендинга

Hero → Ответ с источником → Возможности → Сценарии → **Тарифы** → **Безопасность** → **Отзывы** → **FAQ** → CTA → Футер.

Жирным — новые блоки, которых на текущем сайте нет.

### Что изменилось относительно текущего сайта

| Было | Стало |
|---|---|
| Единственный CTA «Записаться на звонок» | Основной — «Попробовать 7 дней бесплатно», «Записаться на звонок» — вторичный (механика «созвон → +2 недели» сохраняется) |
| Цен нет вообще | Блок «Тарифы» + отдельная страница `/tarify` |
| `/tarify`, `/oferta`, `/privacy`, `/rekvizity` → 404 | Все четыре открываются, ссылки в футере каждой страницы |
| Футер: «© 2026 Джейхелпер» | Юрлицо, ОГРН, ИНН, адрес, документы, контакты, CloudPayments |
| Триал не упомянут | Триал в hero, в тарифах, в CTA и в FAQ |
| Карточка «Конфиденциальность данных» (2 пункта) | Заменена на «Ответы с опорой на закон»; конфиденциальность вынесена в отдельный блок |
| Обещания без доказательств | Блок «Ответ с источником» + блок «Безопасность» только с проверяемыми пунктами |
| Нет FAQ | 7 вопросов на `<details>` |
| `google-site-verification: your-google-verification-code` | Заглушка убрана, вместо неё комментарий в `<head>` |

---

## Реестр TODO — что дозаполнить перед публикацией

В прототипе все незаполненные места помечены жёлтыми плашками `.todo` — их видно глазами. Всего **41 штука** на пяти страницах. Публиковать, пока осталась хоть одна, нельзя.

Пункты 1 и 7 блокируют мониторинг платёжного сервиса.

**Решено:** телефон и часы работы поддержки не публикуем. Единственный оперативный канал — Telegram [@tuzovgleb](https://t.me/tuzovgleb), он проставлен в футере всех пяти страниц и в контактных разделах всех четырёх документов. В исходниках `Юр_документы/*.md` там ещё стоит «Телефон: ⟨указать⟩» — синхронизировать перед публикацией.

| № | Что | Где в прототипе | Чек-лист |
|---|---|---|---|
| 1 | E-mail для обращений пользователей | футер всех 5 страниц, `/rekvizity`, `/tarify`, `/oferta` пп. 6.4 / 7.1 / 15.2 / 16, `/privacy` пп. 8.2 / 11 | пробел №1 |
| 2 | ~~Телефон и часы работы~~ — решено не публиковать, вместо них Telegram | — | пробел №2 закрыт |
| 3 | Название раздела ЛК, где отключается автопродление | FAQ на лендинге, `/tarify` «Оплата и продление», `/oferta` п. 6.4 | пробел №3 |
| 4 | Трансграничная передача: есть или нет — выбрать (а) или (б) | `/privacy` п. 5.6 | пробел №4 |
| 5 | Сроки хранения данных после удаления аккаунта | `/privacy` пп. 6.2 / 6.3, `/oferta` п. 10.5 | пробел №5 |
| 6 | Ответственный за обработку ПДн (ФИО, должность) | `/privacy` п. 11 | пробел №6 |
| 7 | Дата вступления редакций в силу + «цены действуют с» | шапки всех 4 документов | пробел №7 |
| 8 | Что при исчерпании 200 обращений: жёсткий стоп или докупка | FAQ, `/tarify` | пробел №8 |
| 9 | ~~Telegram поддержки~~ — проставлен: @tuzovgleb | футер, все 4 документа | закрыт |
| 10 | Реальный код google-site-verification | `<head>` `index.html` | ТЗ §4.1 |
| 11 | Подтвердить цифры: 8 млн+ дел в базе, 18 команд | hero, блок «Ответ с источником», FAQ | — |
| 12 | Факты под два закомментированных пункта безопасности | блок «Безопасность» | ТЗ §2.6 |
| 13 | 3–6 реальных отзывов + письменное согласие на публикацию | блок «Отзывы» | ТЗ §2.7 |
| 14 | Почтовый адрес для претензий, если отличается от юридического | `/rekvizity` | — |
| 15 | Сроки в оферте: повтор списания ⟨3⟩ дня, недоступность ⟨24⟩ часа, хранение ⟨30⟩ дней | `/oferta` пп. 6.5 / 7.3 / 10.5 | — |

### Служебные плашки, которые надо удалить перед публикацией

Одна розовая плашка `.note` — в блоке «Отзывы», про то, что блок нельзя публиковать с заглушками. В прод не идёт.

### Рабочий экран продукта на первом экране

Интерфейс Джейхелпера в hero свёрстан в HTML/CSS (`.ws-*` в `landing.css`), а не вставлен картинкой: резче на retina, читается поисковиками, не требует поддержки отдельного файла при изменениях UI.

Если нужен именно скриншот — положить PNG в `assets/` и заменить блок `<div class="card-x ws">…</div>` на `<img>`; готовая строка замены лежит комментарием прямо над блоком в `index.html`.

---

## Два важных момента, всплывших при сборке

### 1. Спорные пункты безопасности уже утверждены в самих документах

Два пункта блока «Безопасность» лежат в `index.html` **закомментированными** — «данные хранятся и обрабатываются в России» и «ваши документы не используются для обучения моделей». Но оказалось, что оба уже прямо заявлены в юр-документах:

- **Политика конфиденциальности, п. 5.5:** обработка ПДн граждан РФ ведётся с использованием баз данных, находящихся на территории Российской Федерации.
- **Оферта, п. 10.3:** материалы Пользователя не используются для обучения моделей ИИ без отдельного явного согласия.

То есть вопрос не в том, можно ли это писать на лендинге, а в том, **соответствует ли этим двум пунктам фактическое положение дел** — где физически стоят серверы и что написано в договоре с LLM-провайдером. Если реальность расходится с документами, проблема серьёзнее лендинга: расхождение уже опубликовано в оферте и политике. Проверить нужно в любом случае.

### 2. Роут тарифов: `/tariffs` vs `/tarify`

Источники расходятся:

- `oferta.md` (термин «Тарифы») ссылается на `https://jhelper.ru/tariffs`;
- `checklist-i-soglasie.md` §2 требует `/tariffs`, `/oferta`, `/privacy`, `/requisites`;
- `План_пересборки_сайта_jhelper.md` §3 задаёт `/tarify`, `/oferta`, `/privacy`, `/rekvizity`.

В прототипе взяты роуты из плана пересборки (`/tarify`, `/rekvizity`), и ссылка в тексте оферты приведена к ним. **Перед публикацией зафиксировать один вариант** и синхронно поправить текст оферты в `Юр_документы/oferta.md` — иначе ссылка внутри договора будет вести на 404.

### 3. Ставка НДС: в .docx осталось 20%, в .md уже 22%

В `Юр_документы/` лежат две версии каждого документа, и они разошлись:

- `tarify.md` (11 августа) — «в том числе **НДС 22%**»;
- `Тарифы.docx` (7 августа) — «в том числе **НДС 20%**».

В прототипе взято **22%** — это действующая ставка с 01.01.2026 (ФЗ № 425-ФЗ от 28.11.2025). `.docx` — более ранний экспорт, его надо перевыгрузить из `.md`, иначе у юриста и у эквайера окажутся разные цифры.

В оферте расхождения нет: там нейтральное «с учётом НДС по действующей ставке» в обеих версиях.

### 4. Приложенные .docx новых данных не содержат

Все четыре `.docx` — это Word-экспорт тех же `.md`, снятый раньше. Незаполненные места в них те же самые: в оферте 8 плейсхолдеров, в политике 8, в реквизитах 4, в тарифах 4 — e-mail, даты редакций, сроки, раздел ЛК, ответственный за ПДн. Брать оттуда нечего; единственное отличие — устаревшая ставка НДС (см. выше).

Источником правды для прототипа остаются `.md`-файлы.

---

## Что передать разработчику

1. **Дизайн-токены не трогать.** Блок `:root` в `tokens.css` скопирован из прод-CSS дословно. Классы `.container-x`, `.card-x`, `.app-header`, `.logo`, `.sec-eyebrow` — тоже. Кнопки на проде свёрстаны инлайновыми Tailwind-классами; здесь они вынесены в `.btn`/`.btn-primary`/`.btn-secondary` с теми же значениями (padding `16px 26px`, radius 8px, 17px/500, `--brand-accent` → hover `--brand-accent-hover`).
2. **SSR/SSG обязателен** для лендинга и четырёх юр-страниц. Сейчас лендинг рендерится только на клиенте — при отключённом JS видно «Загрузка…». Это бьёт и по SEO, и по мониторингу платёжного сервиса: страницы должны открываться **без авторизации и без JS**.
3. **Все четыре юр-страницы — публичные.** Сайт закрыт регистрацией, но `/tarify`, `/oferta`, `/privacy`, `/rekvizity` должны открываться анонимному посетителю.
4. **Чекбокс согласия при регистрации** — не проставлен по умолчанию, со ссылками на `/oferta` и `/privacy`. Текст — в `checklist-i-soglasie.md` §3.
5. **Экран оплаты**: до нажатия кнопки видны наименование юрлица, ИНН, стоимость, периодичность списания при автопродлении и способ его отмены. Шаблон текста — в `checklist-i-soglasie.md` §4.
6. **Логотипы платёжных систем** в футере сейчас текстовые чипы — заменить на изображения по требованиям эквайера.
7. Мобильное меню, `.scrolled` на шапке и якоря — в `app.js`, 60 строк без зависимостей. В Next.js это переносится в клиентский компонент шапки.

---

## Проверено в браузере

- Все 5 роутов отдают 200 (на проде `/tarify`, `/oferta`, `/privacy`, `/rekvizity` сейчас отдают 404).
- Консоль чистая, ошибок нет.
- Вычисленные стили совпадают с продом: `--brand-accent` `#7A2E2E`, `.card-x` radius 12px / padding 24px / border `#E5E7EB` / shadow-sm, h1 Lora 62px `-0.744px` / lh 66.34px, `.btn-primary` padding `16px 26px` radius 8px 17px/500, `.sec-eyebrow` 13px/600 uppercase ls `1.04px`, сетка карточек `281px × 4` gap 20px.
- 375px (mobile) и 1280px (desktop): горизонтального переполнения нет ни на лендинге, ни на юр-страницах, сетка `auto-fit` схлопывается в одну колонку, главный CTA в шапке прячется до 640px (как на проде), бургер работает.
- FAQ раскрывается нативным `<details>`, все 16 якорей оглавления оферты резолвятся.
